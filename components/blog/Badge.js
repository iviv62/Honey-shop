const Badge = ({title}) => {
    return (
        <span class="flex items-center px-2 py-1 text-xs font-bold leading-none text-white bg-yellow-600 rounded">{title}</span>
    )
}

export default Badge
